<div class="gw-label ${labelClasses} ${!label ? 'gw-emptyLabel'}" style="?{labelWidth}width:${labelWidth}">${labelIcon}${label}</div>
<div class="gw-value ${valueClasses}" style="${valueStyleBuilder}">
${:children}
</div>