<div class="gw-TileHeader">
    <div class="gw-TitleSection">
        <div class="gw-Title"><span class="gw-TitleText">${title}</span><div :if="badge" class="gw-Badge">${badge}</div></div>
    </div>
    <div :if="actions" class="gw-Action">${actions}</div>
</div>
${:children}
<div class="gw-TileFooter">${viewMoreLink}</div>