<div class="gw-rv ${reflectionApplies ? 'gw-range-readonly-reflector'}" ${systemAttributes}>
  <div :for="option" :in="selectedOptions" class="gw-readOnlyRangeValue gw-RangeValue ${option.retired ? 'gw-retired'}" data-gw-value="${option.value}">
      ${option.icon}
      <div :if="option.label" class="gw-label">${option.label}</div>
  </div>
</div>
